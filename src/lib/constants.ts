// Storage keys
export const STORAGE_KEYS = {
  ARCHITECTURES: 'jarvis-architectures',
  LANGUAGE: 'jarvis-lang',
} as const

// Firebase collection names
export const COLLECTION_NAMES = {
  ARCHITECTURES: 'architectures',
} as const

// Environment flags
export const USE_LOCAL_STORAGE = false
